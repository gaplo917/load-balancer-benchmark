version: '3.7'
services:
  haproxy:
    network_mode: host
    image: haproxy:2.1.2
    ports:
      - 8080:8080
    ulimits:
      nproc: 65535
      nofile:
        soft: 100000
        hard: 100000
    volumes:
      - ./haproxy:/usr/local/etc/haproxy
